let number = 7; // Виберіть число для таблиці множення
let i = 1; // Початкове значення

while (i <= 10) {
    let result = number * i; // Розрахунок результату
    console.log(`${number} * ${i} = ${result}`); // Вивід у консоль
    i++; // Збільшуємо лічильник
}